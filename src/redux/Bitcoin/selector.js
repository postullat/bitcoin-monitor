export const getStartDate = (state) => state.bitcoin.startDate;

export const getEndDate = (state) => state.bitcoin.endDate;

export const getFrequency = (state) => state.bitcoin.frequency;

export const getItems = (state) => state.bitcoin.items;

export const getLastSyncTime = (state) => state.bitcoin.lastSyncTime;
